pub struct NewProtocol {
    MAGIC_NUMBER: u16,
    function: u8,
    ID: u8,
    data_len:u8,
    data: String,
    check_sum: u32,
}

#[derive(Serialize, Deserialize, Debug, Eq, PartialEq)]
pub enum ExampleProtocol {
    WRITE(NewProtocol),
    READ,
    DELETE,
}
